[pytest]
# `ingest/` is the import root, so `from fpl.gameweeks import ...` resolves the same way in
# the tests as it does in run_fpl.py — no sys.path juggling, no packaging ceremony.
pythonpath = .
testpaths = tests
